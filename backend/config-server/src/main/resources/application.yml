server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/itsmrpin2/ab/tree/master/backend/config-server # Git repo holding config files
          username: ${itsmrpin2}
          password: ${ue3NGF@jwHCY@Bd}
          clone-on-start: true
          default-label: master  # branch

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
